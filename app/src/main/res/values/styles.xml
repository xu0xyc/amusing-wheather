<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/windowBackground</item>

        <!-- 改变DrawerToggle默认的DrawerArrow的样式 -->
        <item name="drawerArrowStyle">@style/AppTheme.MyDrawerArrowStyle</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="AppTheme"></style>

    <!-- 改变DrawerToggle默认的DrawerArrow的样式 -->
    <style name="AppTheme.MyDrawerArrowStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="spinBars">true</item><!-- true旋转样式 false非旋转 -->
        <item name="color">@color/white</item>
    </style>
</resources>
